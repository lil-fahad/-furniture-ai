class Recommender:
    """Simple rule-based recommender placeholder."""

    def recommend(self) -> list[str]:
        """Return static recommendations for UI development."""
        return [
            "Add a floor lamp next to the sofa for balanced lighting.",
            "Use a neutral rug to anchor the coffee table.",
            "Place a plant near the window to freshen the layout.",
        ]
